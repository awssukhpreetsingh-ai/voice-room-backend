# This file is superseded by the notifications/ package.
# Python 3 prefers the package directory over this module file,
# so this file is effectively unreachable. It is kept only as a
# historical marker — all logic now lives in notifications/.
